services:
  # Backend service
  - type: web
    name: food-delivery-backend
    env: node
    runtime: node18
    plan: free
    rootDir: Backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production

  # Frontend service
  - type: web
    name: food-delivery-frontend
    env: static
    plan: free
    rootDir: Frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://backend-ordernl.onrender.com

  # Admin service  
  - type: web
    name: food-delivery-admin
    env: static
    plan: free
    rootDir: Admin
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://backend-ordernl.onrender.com